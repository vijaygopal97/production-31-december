# Backend Environment Variables Sample
# Copy this file to .env and update the values

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/Opine

# Server Configuration
PORT=5000
SERVER_IP=your-server-ip-here
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# JWT Configuration (for future authentication)
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# API Configuration
API_VERSION=v1

# Email Configuration 
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=akashconvergentview@gmail.com
SMTP_PASS="cftr siqg zlgh rzzl"

# File Upload Configuration (Optional - for future use)
# MAX_FILE_SIZE=10485760
# UPLOAD_PATH=./uploads

# Rate Limiting (Optional - for future use)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
